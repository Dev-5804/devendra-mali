# Netlify/Vercel Redirects and Rewrites

# SPA fallback - redirect all routes to index.html for client-side routing
/*    /index.html   200

# Optional: Redirect old URLs if you have any
# /old-page    /new-page    301

# Optional: Force HTTPS
# http://*    https://:splat    301!

# Optional: Redirect www to non-www (or vice versa)
# https://www.devendramali.tech/*    https://devendramali.tech/:splat    301!
